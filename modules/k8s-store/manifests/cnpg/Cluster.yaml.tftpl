apiVersion: postgresql.cnpg.io/v1
kind: Cluster
metadata:
  name: ${NAME}
  namespace: ${NAMESPACE}
spec:
  instances: ${INSTANCES}

  storage:
    size: ${STORAGE_SIZE}

  monitoring:
    enablePodMonitor: ${MONITORING_ENABLED}
